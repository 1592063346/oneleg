{
  "name": "deck-stats",
  "version": "1.0.0",
  "description": "卡组比赛数据可视化网站",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npx --yes http-server -p 8080 -c-1 --proxy http://localhost:8080? ."
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
